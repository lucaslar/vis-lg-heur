import { BetaFormalPipe } from './beta-formal.pipe';

describe('BetaFormalPipe', () => {
  it('create an instance', () => {
    const pipe = new BetaFormalPipe();
    expect(pipe).toBeTruthy();
  });
});

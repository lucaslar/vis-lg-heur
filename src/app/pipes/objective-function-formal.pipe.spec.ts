import { ObjectiveFunctionFormalPipe } from './objective-function-formal.pipe';

describe('ObjectiveFunctionFormalPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectiveFunctionFormalPipe();
    expect(pipe).toBeTruthy();
  });
});

import { GammaFormalPipe } from './gamma-formal.pipe';

describe('GammaFormalPipe', () => {
  it('create an instance', () => {
    const pipe = new GammaFormalPipe();
    expect(pipe).toBeTruthy();
  });
});
